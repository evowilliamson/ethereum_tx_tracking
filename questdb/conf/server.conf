# QuestDB Server Configuration
# HTTP server port (Railway requires port 80)
http.bind.to=0.0.0.0:80

# PostgreSQL wire protocol port (default: 8812)
# Used by Python application via psycopg2
# pg.net.bind.to=0.0.0.0:8812

# ILP (InfluxDB Line Protocol) port (default: 9009)
# ilp.net.bind.to=0.0.0.0:9009



